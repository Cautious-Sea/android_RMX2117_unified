# Device Tree Changes

- **BoardConfig.mk**: Set `TARGET_KERNEL_CLANG_VERSION := r530567` to explicitly use the Clang 19 compiler toolchain for kernel compilation, replacing the default `r498229b` version.
